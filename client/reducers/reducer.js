import {ADD_EXERCISE, ADD_EXERCISE, ADD_FOOD, ADD_SUMMARY} from '../constants/actionTypes';

const initialState = {

};

export const setExercise = (state = initialState, action = {}) => ({
    switch(action.type){
        case 'ADD_EXERCISE' :
            
    }
});

